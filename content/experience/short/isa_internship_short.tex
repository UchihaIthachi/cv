\CVExperienceEntry
  {Software Engineer Intern}
  {Dec 2024 -- May 2025}
  {ISA (Information Systems Associates)}
  {Colombo, Sri Lanka}
\begin{itemize}[leftmargin=*, itemsep=0.35em]
  \item Engineered a distributed, high-throughput ID generator for the AeroOrder microservice using \textbf{Java 18}, \textbf{Spring Boot}, and a \textbf{Kafka}-backed pool model, ensuring uniqueness across scaled instances.
  \item Optimized service performance to achieve \textbf{25+ operations/sec throughput} per node with \textbf{<100ms latency}, validating resilience via extensive \textbf{Apache JMeter} load testing and \textbf{Prometheus/Grafana} monitoring.
  \item Architected a hybrid \textbf{gRPC} and \textbf{Kafka} integration strategy within a legacy \textbf{Java 8} monolithic reservation system, enabling seamless, backward-compatible communication via database-driven feature toggles.
  \item Resolved critical production bottlenecks by refactoring inefficient \textbf{Kafka Streams} logic into a robust Producer-Consumer model, drastically reducing consumer lag and improving fault tolerance.
\end{itemize}
